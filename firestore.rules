rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/progress/{courseId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    match /users/{userId}/rewards/{docId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
